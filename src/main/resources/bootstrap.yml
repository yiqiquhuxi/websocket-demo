spring:
  cloud:
    nacos:
      config:
        namespace: dev
        server-addr: http://chenqi.space:8848
        file-extension: yaml
        extension-configs[0]:
          dataId: rabbitmq-single.yaml
          group: mybatis-plus
          refresh: true
